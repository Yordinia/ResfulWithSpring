package com.ResfulWithSpring.repositories;

import com.ResfulWithSpring.models.EndStep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EndStepRepository extends JpaRepository<EndStep, Long> {
}
